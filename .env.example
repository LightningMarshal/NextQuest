# Node-side tooling only (drizzle-kit generate/migrate runs under Node, not
# Workers, so it cannot read .dev.vars). Copy to .env and keep DATABASE_URL
# in sync with .dev.vars.
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/stooge_log?sslmode=require
